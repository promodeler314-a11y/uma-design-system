# DESIGN.md：ウマ娘関連ツール共通デザイン

対象：レースエミュレータ、F-13（馬券購入・レース運営）、ローテーション最適化

## 1. 利用者
- レースエミュレータ：一般公開のツールの利用者
- F-13・ローテーション最適化：サークルのメンバーと運営

## 2. 方向性
- 土台：レースエミュレータの既存トークン体系
- アクセシビリティ：デジタル庁デザインシステムの基準を参照する（参照・転載する場合は出典を明記する）
- モチーフ：競馬新聞・出馬表の紙面を骨格とする。JRA のマークカードの色分け（緑・青・赤）をカテゴリの色分けに、競馬場の着順掲示板を結果表示の局所的な演出に用いる
- 特定の競馬新聞の題字や紙面構成の模倣、JRA・ウマ娘のロゴや画像などの権利物の使用はしない

## 3. 配色
- レースエミュレータの既存配色を引き継ぎ、強調色として朱を加える
- 色だけで情報を伝えない。色を使う区別には必ず文字を併記する

## 4. 書体
- 本文・見出し：BIZ UDPゴシック（太さは 400 と 700 のみ）
- 数値：等幅数字で揃える。BIZ UDPゴシックの tabular-nums 対応を検証し、非対応なら数値の列に BIZ UDゴシックを使う
  - 検証の結果、非対応だった（tabular-nums を指定しても数字の幅が変わらない）。数値の列には BIZ UDゴシックを使う（`--uma-font-mono`）
- 見出しの階層は、文字の大きさ・太さ2段階・罫線で表現する
- 読み込みは Google Fonts

## 5. 文体
- 文章（通知・説明・空の状態）は敬体
- 見出し・ラベル・ボタンは体言止め

## 6. アクセシビリティ
- 文字と背景のコントラスト比は WCAG 2.x の AA を満たす
- 操作できる要素には必ずフォーカス表示を設ける
- prefers-reduced-motion の設定を尊重する

## 7. 禁止事項
- 共通の禁止パターンは ui-ban-list スキルに従う

## 8. 未決定事項

`tokens.css` に入っている値は、レースエミュレータの実装から移した暫定のものである。
2 節の「土台」をそのまま写しただけで、下記はまだ決めていない。

- 配色の具体的な値（暫定値は `tokens.css`。強調色の朱はまだ入れていない）
- トークン名の統一（接頭辞は `--uma-` にした。名前そのものは移す前のまま）
- 角丸・余白・文字サイズの値（角丸と書体は暫定値が `tokens.css` にある。余白は各リポジトリの既定のまま）
- F-13 のダーク表示への対応
- レースエミュレータの近似の印（△▲）の代替記号（競馬新聞の予想印と衝突するため）
- 入力のまとまりの表現、空値の表記、ピル型部品の扱い、画面幅
